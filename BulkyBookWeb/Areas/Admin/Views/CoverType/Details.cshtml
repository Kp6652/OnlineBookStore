@model BulkyBook.Models.CoverType

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>CoverType</h4>
    <hr />
    <dl class="row">
        <dt class"col-sm-2">
            @Html.DisplayNameFor(model=>model.ID)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model=>model.ID)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
            </dl>
</div>
<div class="w-75 btn-group" role="group">
                    <a asp-controller="CoverType" asp-action="Edit" class="btn btn-primary mx-2" asp-route-id="@Model.ID">
                        <i class="bi bi-pencil-square"></i>&nbsp; Edit</a>
                       
                    <a asp-controller="CoverType" asp-action="Delete" class="btn btn-danger mx-2" asp-route-id="@Model.ID">
                        <i class="bi bi-trash-fill"></i>&nbsp; Delete</a>
                    <a asp-controller="CoverType" asp-action="Index" class="btn btn-secondary" style="width=150px">
                        <i class="bi bi-skip-backward-circle-fill"></i>&nbsp;Back to List</a>
</div>
